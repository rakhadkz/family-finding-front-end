import { ErrorMessage } from "@atlaskit/form";
import styled from "styled-components";

export const StyledTextError = styled(ErrorMessage)`
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  line-height: 6px;
`;
