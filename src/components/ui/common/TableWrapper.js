import styled from "styled-components";

export const TableWrapper = styled.div`
  table {
    th {
      color: #6b778c;
      font-style: normal;
      font-weight: normal;
      font-size: 12px;
      line-height: 16px;
    }

    td {
      padding-top: 10px;
      padding-bottom: 10px;
      font-style: normal;
      font-weight: normal;
      font-size: 14px;
      line-height: 20px;
      color: #172b4d;
    }
  }
`;
